version: 2.1

orbs:
  node: circleci/node@4.7.0

jobs:
  build:
    working_directory: Sprint_3/Software Engineering CW/se-cw
    docker:
      - image: cimg/node:lts
    steps:
      - checkout:
          path: Sprint_3/Software Engineering CW
      - run: npm install -g supervisor && npm install && npm install supervisor
